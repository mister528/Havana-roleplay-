# Append this block to data/handling.cfg
# (Both PC and Android: assets/data/handling.cfg inside HavanaRp.apk OR the
# player's GTA SA Mobile install — replace the line whose first column is
# AZELOW; if it does not exist, append it at the end of the vehicle section.)
#
# Reference handling: borrowed from STALLION (id 439) and tuned slightly for the
# Daewoo Gentra Azelow profile (compact 4-door sedan, FWD, low mass, mid grip).
#
# Columns (per handling.cfg spec):
# A    B(mass)  C(turn)   D(dragMlt)  E(CMx) E(CMy) E(CMz)  F(perOcc)  G(maxOcc)
# H(fSubLin) I(driveTrac) J(MonV) K(steerLk) L(driveBias) M(brakeBias) N(brakeMlt)
# O(transmission) P(antiDive) Q(seatOff) R(value) S(suspForceLevel) T(suspDamping)
# U(suspBias) V(transmission) W(modelFlags hex) X(handlingFlags hex) Y(frontLights) Z(rearLights) AA(animgrp)

AZELOW       1450.0 5500.0  4500.0  4500.0   0.0  0.20 -0.30   75   0.85 0.80  35.0 1.0 1.0 5 175.0 0.50 R P  9.5  0.45 35.0  0.4  1.6 0.10 -0.15 0.5  0.40 35000 0x00204004 0xC00000 1 1 0
